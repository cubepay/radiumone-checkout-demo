# Next.js loads .env.local automatically — see Next.js docs.
# Copy this file to .env.local and fill in your sandbox credentials.
# See ../../.env.example for the full canonical list with descriptions.
# RADIUMONE_BASE_URL is the public sandbox host; for the production host ask your RadiumOne contact.

RADIUMONE_BASE_URL=https://checkout-sandbox.radiumone.com
RADIUMONE_SECRET_KEY=r1sk_test_replace_me
MERCHANT_BASE_URL=http://localhost:3000

# OPTIONAL — only needed to verify Gateway webhooks at POST /api/webhook (Step 4).
# RADIUMONE_WEBHOOK_SECRET=whsec_replace_me
